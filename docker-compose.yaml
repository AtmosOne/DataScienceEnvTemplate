version: '3'
services:

  jupyter-notebook:
    image: atmosphere4u/jupyter-notebook-environments:dark
    ports:
      - 5000:5000
    volumes:
      - ./notebooks:/notebooks
      - ./data:/data
      - ./models:/models
      - ./scripts/jupyter-startup.sh:/jupyter-startup.sh
    network_mode: host
    command: bash -c "source jupyter-startup.sh"
    restart: always